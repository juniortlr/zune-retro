Ticket H2. Role: independent adversarial test designer for an OFFLINE Windows launcher evidence validator. No tools. Return a concise Markdown decision table of valid and invalid synthetic cases and expected rejection codes. Never claim execution or project approval. The table will inform a separately reviewed implementation; do not produce fake runtime evidence.

Canonical campaign: 1,000 scored activations in five groups of 200: hotkey toggles (100 hidden->visible,100 visible->hidden); CLI toggles same split; sequential CLI show (100 hidden->visible,100 visible->visible); sequential CLI hide (100 visible->hidden,100 hidden->hidden); concurrent IPC (40 batches of 5 individual requests:20 show batches starting hidden and20 hide batches starting visible). Setup/preconditioning recorded separately and excluded. Placement is 100 observations INCLUDED in the 1000, two monitors, 25 hotkey and25 CLI/integrated each. At least one negative-coordinate topology. Each expected state observed within500ms. Exactly1 resident process, at most1 menu HWND; each secondary exits within1s. Every IPC request has one matching result; hotkeys have real observed state and no invented ACK. Actual application order must be observed, not reconstructed by sorting planned sequence. Bounds tolerance2 physical pixels, DPI tolerance0.01. Every required missing/failed/skipped case prevents pass. Manifest must bind source/diff, build/fixture hashes and environment; no private fields, free text, actual inventory. Warm/cold performance is separate and must not be derived from this campaign. Complete campaign alone does NOT qualify whole project: accessibility, security, recovery, packaging and owner decisions remain separate.

Include counterexamples catching double-counted batches, placement added as extra activations, setup counted as scored, duplicate request IDs, duplicate results, missing tail failures, forged ordering, wrong transitions inside concurrent batches, hotkey fake ACK, mismatched topology, negative coordinates incorrectly rejected, incorrect boundary inequalities, boolean numbers, missing native-fallback observations, and missing broader evidence. Return at most25 rows and clearly identify missing schema decisions that primary must resolve, not invent acceptance policy.
