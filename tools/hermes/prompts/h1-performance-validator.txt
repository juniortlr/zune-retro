Ticket H1a. Role: implement an OFFLINE evidence validator for a Windows launcher feasibility project. You have no tools. Return exactly two complete fenced Python files labeled validator.py and test_validator.py. Python 3 standard library only. Do not claim you ran tests. No unrelated prose or architecture expansion. These are proposed files; primary will inspect and test before integration.

This first ticket covers only manifest and separate performance samples, NOT full project qualification. validate(packet) returns a list of fixed error codes (never echo private input). CLI reads UTF-8 JSON from one path, emits JSON {"valid": bool, "errors": [...]}, exits 0 iff valid, 1 for invalid, 2 for input/JSON error. Reject duplicate JSON keys and NaN/Infinity. Never execute or install anything or write the input. Max input 2 MiB; reject over limit before reading all content.

Strict schema, no extra keys anywhere:
root keys: schema_version (integer 1, not bool), synthetic (bool), manifest, performance.
manifest keys: source_commit (40 lowercase hex), dirty (bool), patch_sha256 (64 lowercase hex if dirty, otherwise null), binary_sha256 and fixture_sha256 (64 lowercase hex), environment.
environment exact keys: os_build (numeric dotted version string, max 32 chars), sdk (numeric dotted version string max 32), runtime (same), integrity (literal medium), recorded_utc (strict timezone-aware ISO-8601 UTC timestamp ending Z).
performance exact keys: warm and cold. Each exact keys: warmups_discarded, samples_ms, reported_p95_ms. warm: exactly 20 warmups discarded, exactly 200 finite nonnegative numeric samples, p95 <=150 ms. cold: 5 warmups discarded, exactly 50 finite nonnegative samples, p95 <=1000 ms. Reject bool as number. Nearest-rank p95 = sorted(samples)[ceil(0.95*n)-1]. Reported p95 must equal recomputed p95 within absolute 1e-9. All samples count, no filtering or exclusions. Missing/failed samples represented by null invalidate. No user names, machine names, file paths, free text, inventory or arbitrary nested fields accepted. Type violations must return codes instead of raising unexpected exceptions. synthetic=true permits validation of synthetic fixtures, never represents measured project evidence.

Provide meaningful unittest cases for valid synthetic packet, p95 boundary using a tail above threshold, incorrect nearest-rank claims, missing/null samples, bool numerics, unknown/private fields at nested levels, bad hashes, dirty patch requirement, invalid timestamps, malicious nested type replacements, duplicate keys, nonfinite JSON and oversized input. Make test fixture construction deterministic and clearly synthetic. Keep implementation readable and bounded. Command will be python -m unittest discover -s tools/evidence-validator -p test_*.py. No cloud services in generated tool. Do not access or request secrets, actual app inventories, raw test logs, network, UI or system configuration.
